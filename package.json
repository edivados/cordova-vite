{
  "name": "vite-plugin-cordova-monorepo",
  "private": true,
  "scripts": {
    "prepare:android": "npm run --workspace=example prepare:android",
    "prepare:ios": "npm run --workspace=example prepare:ios",
    "dev:android": "npm run --workspace=example dev:android",
    "dev:ios": "npm run --workspace=example dev:ios",
    "build:android": "npm run --workspace=example build:android",
    "build:ios": "npm run --workspace=example build:ios",
    "preview:android": "npm run --workspace=example preview:android",
    "preview:ios": "npm run --workspace=example preview:ios"
  },
  "workspaces": ["vite-plugin-cordova", "example"]
}
